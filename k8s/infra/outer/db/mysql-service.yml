apiVersion: v1

kind: Service

metadata:
  # Cluster 내부에서 접근하는 이름 Ex jdbc:mysql/mysql:3306
  name: mysql
spec:
  selector:
    app: mysql
  type : NodePort
  ports:
    - targetPort: 3306
      port: 3306
      nodePort: 33000     # 외부 접근 고정 NodePort (30000-32767)
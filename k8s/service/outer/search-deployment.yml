apiVersion: apps/v1
kind: Deployment
metadata:
  name: hexagon-search-service  # inputs.service-name 대체
  labels:
    app: hexagon-search-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hexagon-search-service
  template:
    metadata:
      labels:
        app: hexagon-search-service
    spec:
      containers:
        - name: hexagon-search-service
          image: kimtaewanpea/hexagon-search-service:latest  # GitHub Actions 변수 대체
          ports:
            - containerPort: 8087  # 서비스 포트 필요시 수정
          envFrom:
            - secretRef:
                name: mysql-secret
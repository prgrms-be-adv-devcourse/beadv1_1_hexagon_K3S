apiVersion: apps/v1
kind: Deployment
metadata:
  name: hexagon-contract-service  # inputs.service-name 대체
  labels:
    app: hexagon-contract-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hexagon-contract-service
  template:
    metadata:
      labels:
        app: hexagon-contract-service
    spec:
      containers:
        - name: hexagon-contract-service
          image: kimtaewanpea/hexagon-contract-service:latest  # GitHub Actions 변수 대체
          ports:
            - containerPort: 8083  # 서비스 포트 필요시 수정
          envFrom:
            - secretRef:
                name: mysql-secret